import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CrearJuegoElegirEmocionPage } from './crear-juego-elegir-emocion.page';

describe('CrearJuegoElegirEmocionPage', () => {
  let component: CrearJuegoElegirEmocionPage;
  let fixture: ComponentFixture<CrearJuegoElegirEmocionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CrearJuegoElegirEmocionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
